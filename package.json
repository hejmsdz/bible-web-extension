{
  "name": "bible-web-extension",
  "version": "1.0.0",
  "description": "Quickly find Bible verses from your browser's address bar",
  "main": "index.js",
  "scripts": {
    "start": "web-ext run --source-dir ./src/",
    "build": "web-ext build --source-dir ./src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mikołaj Rozwadowski",
  "license": "ISC",
  "devDependencies": {
    "web-ext": "^5.5.0"
  }
}
